# 安全说明

## 报告安全问题

自用项目：发现安全问题请直接联系维护者，不要公开披露。

## 已知边界（v0.1.0）

- 传输协议默认依赖 HTTPS / HTTP/3（TLS）；禁止以明文 HTTP 暴露公网；
- v0.1.0 核心引擎不包含服务端加密与鉴权，计划在 v0.5.0 引入；
- 对象完整性由 SHA256 保证，写入与读取均可校验；
- 键名通过哈希目录隔离，路径穿越不可达。

## 安全默认值

- 原子写：先写临时文件并 fsync，再 rename，避免半成品对象；
- 元数据与数据分离，损坏元数据不会被误当成正常对象；
- 删除操作直接移除数据与元数据（v0.4.0 起支持软删除/版本化）。
